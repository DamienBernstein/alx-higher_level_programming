#!/usr/bin/python3
def element_at(my_list, idx):
  for arr in range(len(my_list)):
    if (idx < 0):
      return (none)
    if (idx > len(my_list)):
      return (none)
    elif (idx == array):
      return (my_list[arr])
  
